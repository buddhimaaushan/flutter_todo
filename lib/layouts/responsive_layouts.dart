import 'package:flutter/material.dart';

class R1xCol extends StatelessWidget {
  final Widget child;
  const R1xCol({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
